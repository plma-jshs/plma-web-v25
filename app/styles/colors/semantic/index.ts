import type { NestedColors } from "../primitives"
import { type SemanticColorType } from "./_base"

export const semanticColors: Record<SemanticColorType[number], NestedColors> = {
    "background-basic-default-default": "carbonBlack.100",
}
